# FastAPI Backend Dependencies
# These are IN ADDITION to existing requirements.txt in root

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0  # ASGI server with auto-reload
python-multipart==0.0.12  # For file uploads

# Data validation (versions match parent requirements.txt)
pydantic>=2.5.0
pydantic-settings>=2.6.0
