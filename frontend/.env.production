# Production Environment Variables
#
# For Azure deployment: Use relative URL (empty string) so frontend calls same domain
# The backend API and frontend static files are served from the same Azure Web App

VITE_API_URL=
