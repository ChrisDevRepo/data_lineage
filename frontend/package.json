{
  "name": "data-object-lineage",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "Interactive data lineage visualizer for Microsoft SQL Server family (SQL Server, Azure SQL, Synapse, Fabric)",
  "author": "Christian Wagner",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host",
    "build:azure": "npm run build && npm run deploy:prepare",
    "deploy:prepare": "cp deploy/web.config dist/ && echo 'Deployment package ready in dist/'",
    "deploy:zip": "npm run build:azure && cd dist && zip -r ../deploy.zip . && cd .. && echo 'Created deploy.zip'",
    "serve": "npm run build && npm run preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist deploy.zip node_modules/.vite"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.7.0",
    "dagre": "0.8.5",
    "graphology": "^0.26.0",
    "graphology-traversal": "^0.3.1",
    "lucide-react": "^0.554.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "reactflow": "^11.11.4"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
