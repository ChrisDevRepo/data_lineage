name: CI Validation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  # ============================================================================
  # Job 1: Backend Tests (Python)
  # ============================================================================
  backend-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Need history for git diff

    - name: Check if backend files changed
      id: check_backend
      run: |
        # Skip if commit message contains [skip ci]
        if echo "${{ github.event.head_commit.message }}" | grep -q "\[skip ci\]"; then
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "⏭️  Skipping - [skip ci] in commit message"
          exit 0
        fi

        # Always run on pull requests
        if [ "${{ github.event_name }}" == "pull_request" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Running - pull request event"
          exit 0
        fi

        # Check if Python backend files changed
        if git diff --name-only HEAD~1 HEAD | grep -E '^(lineage_v3/|api/|tests/|requirements\.txt)'; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Backend files changed"
        else
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "⏭️  No backend files changed"
        fi

    - name: Set up Python 3.10
      if: steps.check_backend.outputs.changed == 'true'
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      if: steps.check_backend.outputs.changed == 'true'
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov

    - name: Run unit tests with coverage
      if: steps.check_backend.outputs.changed == 'true'
      run: |
        echo "🧪 Running unit tests..."
        pytest tests/unit -v --cov=lineage_v3 --cov=api --cov-report=xml --cov-report=term

    # Integration tests removed from CI - require production database (lineage_workspace.duckdb)
    # Run locally: pytest tests/integration -v

    - name: Upload coverage to Codecov (optional)
      if: steps.check_backend.outputs.changed == 'true' && github.event_name == 'pull_request'
      uses: codecov/codecov-action@v4
      with:
        files: ./coverage.xml
        flags: backend
        name: backend-coverage
      continue-on-error: true  # Don't fail if codecov is down

  # ============================================================================
  # Job 2: User-Verified Test Cases (Infrastructure Only)
  # ============================================================================
  user-verified-cases:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pyyaml

    - name: Run user-verified test cases
      run: |
        echo "🧪 Running user-verified test cases..."
        echo "   (Tests will skip if no cases exist - infrastructure ready)"
        pytest tests/unit/test_user_verified_cases.py -v --tb=short
      continue-on-error: true  # Don't fail CI - infrastructure only, no cases yet

  # Parser Baseline Validation removed from CI - requires production database
  # Run locally: python scripts/testing/check_parsing_results.py

  # ============================================================================
  # Job 3: Frontend Tests & Linting
  # ============================================================================
  frontend-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Need history for git diff

    - name: Check if frontend files changed
      id: check_frontend
      run: |
        # Skip if commit message contains [skip ci]
        if echo "${{ github.event.head_commit.message }}" | grep -q "\[skip ci\]"; then
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "⏭️  Skipping - [skip ci] in commit message"
          exit 0
        fi

        # Always run on pull requests
        if [ "${{ github.event_name }}" == "pull_request" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Running - pull request event"
          exit 0
        fi

        # Check if frontend files changed
        if git diff --name-only HEAD~1 HEAD | grep -E '^frontend/'; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Frontend files changed"
        else
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "⏭️  No frontend files changed"
        fi

    - name: Set up Node.js
      if: steps.check_frontend.outputs.changed == 'true'
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install dependencies
      if: steps.check_frontend.outputs.changed == 'true'
      run: |
        cd frontend
        npm ci

    - name: Type check
      if: steps.check_frontend.outputs.changed == 'true'
      run: |
        cd frontend
        npm run type-check

    - name: Run unit tests (if configured)
      if: steps.check_frontend.outputs.changed == 'true'
      run: |
        cd frontend
        # npm test -- --run (once Vitest is set up)
        echo "⚠️  Frontend unit tests not yet configured"

  # Frontend E2E tests removed from CI - require running backend API
  # Run locally: cd frontend && npm run test:e2e

  # ============================================================================
  # Job 4: Code Quality Checks
  # ============================================================================
  code-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install quality tools
      run: |
        pip install ruff mypy

    - name: Run Ruff (linting)
      run: |
        ruff check lineage_v3/ api/ scripts/ --output-format=github
      continue-on-error: true  # Don't fail build, just report

    - name: Run MyPy (type checking)
      run: |
        mypy lineage_v3/ --ignore-missing-imports --no-strict-optional
      continue-on-error: true  # Don't fail build, just report

  # ============================================================================
  # Job 7: Documentation Validation
  # ============================================================================
  docs-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Need history for git diff

    - name: Check if docs changed
      id: check_docs
      run: |
        # Skip if commit message contains [skip ci]
        if echo "${{ github.event.head_commit.message }}" | grep -q "\[skip ci\]"; then
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "⏭️  Skipping - [skip ci] in commit message"
          exit 0
        fi

        # Always run on pull requests
        if [ "${{ github.event_name }}" == "pull_request" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Running - pull request event"
          exit 0
        fi

        # Check if docs files changed
        if git diff --name-only HEAD~1 HEAD | grep -E '^docs/'; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Docs files changed"
        else
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "⏭️  No docs files changed"
        fi

    - name: Check for outdated phantom config references
      if: steps.check_docs.outputs.changed == 'true'
      run: |
        echo "🔍 Checking for outdated PHANTOM_INCLUDE_SCHEMAS references..."
        if grep -r "PHANTOM_INCLUDE_SCHEMAS" docs/ --include="*.md"; then
          echo "❌ Found outdated PHANTOM_INCLUDE_SCHEMAS references in docs!"
          echo "   Should use PHANTOM_EXTERNAL_SCHEMAS (v4.3.3)"
          exit 1
        else
          echo "✅ No outdated phantom config references found"
        fi

    - name: Check for version consistency
      if: steps.check_docs.outputs.changed == 'true'
      run: |
        echo "🔍 Checking version consistency in docs..."
        VERSION_REFS=$(grep -roh "v4\.[0-9]\.[0-9]" docs/ | sort | uniq)
        echo "Found version references: $VERSION_REFS"

        # Check if latest version (v4.3.3) is present
        if echo "$VERSION_REFS" | grep -q "v4.3.3"; then
          echo "✅ Latest version v4.3.3 found in docs"
        else
          echo "⚠️  Latest version v4.3.3 not found in all docs (this may be OK for historical references)"
        fi

  # ============================================================================
  # Summary Job (Required for PR merge)
  # ============================================================================
  ci-success:
    runs-on: ubuntu-latest
    needs: [backend-tests, frontend-tests]
    if: always()

    steps:
    - name: Check all jobs
      run: |
        # Jobs can be "success" (passed) or "skipped" (no relevant files changed)
        # Both are acceptable outcomes. Only "failure" or "cancelled" should fail the build.

        echo "Job results:"
        echo "  backend-tests: ${{ needs.backend-tests.result }}"
        echo "  frontend-tests: ${{ needs.frontend-tests.result }}"

        # Check for failures (skipped is OK)
        if [ "${{ needs.backend-tests.result }}" == "failure" ] || \
           [ "${{ needs.frontend-tests.result }}" == "failure" ]; then
          echo "❌ One or more required jobs failed"
          exit 1
        fi
        echo "✅ All CI checks passed (some may have been skipped)!"
